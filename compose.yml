---
name: odoo-localdev

services:
  proxy:
    hostname: proxy
    image: traefik:v3.6.2
    restart: "no"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /etc/localtime:/etc/localtime:ro
      - ./config/traefik/traefik.yml:/etc/traefik/traefik.yml:ro
      - ./certs/:/etc/certs/
    ports:
      - "80:80"
      - "443:443"
      - "127.0.0.1:81:8080"
    networks:
      - public

  web:
    hostname: web
    image: marvinsoftwaresolutions/odoo:16.0
    restart: "no"
    labels:
      traefik.enable: true
      traefik.http.routers.odoo.entrypoints: web, websecure
      traefik.http.routers.odoo.rule: Host(`${ODOO_FQDN}`)
      traefik.http.routers.odoo.tls: true
      traefik.http.routers.odoo.tls.domains[0].main: '*.${DOMAIN}'
      traefik.http.routers.odoo.tls.domains[1].main: '${DOMAIN}'
      traefik.http.services.odoo.loadbalancer.sticky.cookie.httpOnly: "true"
      traefik.http.services.odoo.loadbalancer.sticky.cookie.secure: "true"
    depends_on:
      - database
    environment:
      HOST: ${DATABASE_HOST:-database}
      USER: ${DATABASE_USER:-odoo}
      PASSWORD: ${DATABASE_PASSWORD:-odoo}
    networks:
      - private
      - public

  database:
    hostname: database
    image: postgres:14
    restart: "no"
    labels:
      traefik.enable: true
      traefik.http.routers.db.entrypoints: web, websecure
      traefik.http.routers.db.rule: Host(`${DATABASE_FQDN}`)
      traefik.http.routers.db.tls: true
      traefik.http.routers.db.tls.domains[0].main: '*.${DOMAIN}'
      traefik.http.routers.db.tls.domains[1].main: '${DOMAIN}'
      traefik.http.services.db.loadbalancer.sticky.cookie.httpOnly: "true"
      traefik.http.services.db.loadbalancer.sticky.cookie.secure: "true"
    environment:
      POSTGRES_DB:  ${DATABASE_NAME:-postgres}
      POSTGRES_USER: ${DATABASE_USER:-odoo}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD:-odoo}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - private
    ports:
      - "5432:5432"
    healthcheck:
      test: [
        "CMD-SHELL",
        "pg_isready",
        "-q",
        "-d", "${DATABASE_NAME:-database}",
        "-U", "${DATABASE_USER:-odoo}"
      ]
      retries: 3
      timeout: 5s
      interval: 15s
      start_period: 30s
      start_interval: 2s

volumes:
  postgres-data:

networks:
  private:
  public:
